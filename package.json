{
  "engines": {
    "node": "16.14.2",
    "npm": "8.5.0"
  },
  "scripts": {
    "start": "cd url-shortener-backend && npm run start",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false cd url-shortener-frontend && npm install && npm run build"
  }
}
